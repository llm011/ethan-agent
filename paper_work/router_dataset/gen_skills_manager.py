#!/usr/bin/env python3
"""生成 skills-manager 训练样本（jsonl）。

skills-manager = 用 `npx skills` 管理 Agent 技能包：搜/装/卸/更/列。

子语义（现有 + 未来）：
  A 搜/找技能         B 安装技能（GitHub/npm/全局/项目）  C 卸载/删除技能
  D 更新/升级技能      E 列出已装/查看清单               F 概念/范围（内置 vs 用户、要不要重启）
  G 装完用不了/排错    H 从某来源装（给了 url/名字）

★ 三池独立（防近邻泄漏，这是本文件与旧版最大区别）：
  POOL_TRAIN：覆盖广、多变体（含语气后缀展开），主训练用。
  POOL_VAL  ：每子语义换一批说法（中等难度），不与 train 模板骨架重叠。
  POOL_TEST ：每子语义最口语、最贴近真实用户——带背景/上下文/省略/隐式意图，
              句子长短不一，部分故意写长。绝不与 train 共用模板。
  三池分别手写，main() 先建 test/val（共享 seen 占位），再建 train（让路），
  从根上消除「同模板换实体/换后缀」造成的跨池近邻泄漏。

铁律：三池所有样本绝不含任一 trigger 原词子串：
  install skill | add skill | skills list | skills find | 安装技能 | 添加技能
  | 技能管理 | npx skills | skill 包 | search skills | 装技能 | 装个技能 | 卸载技能
  | 删除技能 | 升级技能 | 更新技能 | 技能包 | 能力包 | 找技能 | 搜技能 | 技能列表
  | 装能力 | 加能力
（几乎所有「技能+动词」组合都被屏蔽 → 用「能力/插件/扩展/本事/功能模块/组件」等同义替换）
"""
from __future__ import annotations
import json
import random
from pathlib import Path
from collections import Counter

TRIGGERS = [
    "install skill", "add skill", "skills list", "skills find", "安装技能", "添加技能",
    "技能管理", "npx skills", "skill 包", "search skills", "装技能", "装个技能",
    "卸载技能", "删除技能", "升级技能", "更新技能", "技能包", "能力包", "找技能",
    "搜技能", "技能列表", "装能力", "加能力",
]

# ===================== POOL_TRAIN：覆盖广、多变体 =====================
POOL_TRAIN: dict[str, list[str]] = {
    # ===== A. 搜/找能力 =====
    "find": [
        "帮我看看有哪些现成的扩展能装",
        "搜一下有没有处理 PDF 的插件",
        "想找个能查天气的功能模块",
        "有没有现成的本事可以加给 agent",
        "帮我搜搜社区里有啥好用的扩展",
        "想找个翻译用的插件",
        "有没有能操作数据库的现成模块",
        "帮我查查有没有爬虫相关的扩展",
        "市面上有哪些可以装的小工具",
        "想看看有没有发邮件的功能可以加",
        "帮我找个能画图的扩展",
        "有没有现成的本事能处理表格",
        "搜下有没有股票行情的插件",
        "想找个能识别图片的模块",
        "帮我看看有没有日程管理的扩展",
        "有没有能连接 notion 的现成功能",
        "帮我搜搜跟 github 相关的插件",
        "想找个语音转文字的扩展",
        "有没有能做数据可视化的模块",
        "帮我看看有哪些热门的扩展可以加",
        "想找个能定时提醒的扩展",
        "有没有现成的本事能读取网页",
        "帮我搜搜跟 excel 处理相关的模块",
        "想找个能管理待办的插件",
        "有没有能调用地图的扩展",
        "帮我看看有没有 OCR 识别的功能",
        "想找个能压缩文件的小工具",
        "有没有处理音频的现成扩展",
    ],
    # ===== B. 安装能力 =====
    "install": [
        "帮我把这个扩展装上",
        "给 agent 加一个新本事",
        "我想装一个处理图片的插件",
        "把这个 github 上的扩展装进来",
        "帮我装个能查文档的功能模块",
        "想给它添个发邮件的本事",
        "把这个工具装到全局所有项目都能用",
        "只在当前项目装这个扩展",
        "帮我从 npm 装一个插件",
        "我找到一个好用的扩展，帮我装上",
        "给 agent 装个画图的能力",
        "想把这个第三方模块加进来",
        "帮我装一下这个社区做的扩展",
        "把这个本事装成全局的",
        "我想给它扩充一个新功能",
        "帮我安装一下那个翻译插件",
        "把这个能力添加到 agent 上",
        "想装个能操作 excel 的扩展",
        "帮我把这个仓库里的扩展装进来",
        "给我的 agent 加上爬虫的本事",
        "帮我装个能读 PDF 的扩展",
        "想给它加个连数据库的能力",
        "把这个能发短信的插件装上",
        "帮我装一个做思维导图的模块",
        "想给 agent 添个查快递的本事",
        "把这个语音合成的扩展装进来",
        "帮我装个能跑定时任务的功能",
        "想加个能抓股价的扩展",
        "把这个 OCR 插件装上试试",
    ],
    # ===== C. 卸载/删除能力 =====
    "remove": [
        "帮我把这个扩展卸了",
        "不想要这个本事了，删掉",
        "把那个没用的插件移除",
        "帮我把之前装的那个功能模块删了",
        "这个扩展太占地方，卸载吧",
        "把这个用不上的能力去掉",
        "帮我清掉那个出问题的插件",
        "想把这个扩展彻底删除",
        "把之前加的那个本事撤掉",
        "帮我移除这个不需要的模块",
        "这个插件有 bug，先卸了",
        "把那个重复的扩展删掉一个",
        "帮我把测试用的那个能力清掉",
        "不用的扩展帮我都卸了",
        "把这个旧的功能模块删除",
        "帮我把冲突的那个插件下掉",
        "这个组件太老了，帮我拆掉",
        "把误装的那个本事撤销掉",
        "帮我清理掉那个占空间的模块",
        "这个第三方扩展不想留了，移除吧",
    ],
    # ===== D. 更新/升级能力 =====
    "update": [
        "帮我把这个扩展更到最新版",
        "这个插件有新版本，升一下",
        "把所有装的本事都更新一遍",
        "帮我升级那个翻译扩展",
        "这个功能模块旧了，更新下",
        "把这个扩展拉到最新",
        "帮我检查下有没有可以更新的插件",
        "想把这个能力升到新版本",
        "把过时的扩展都刷新一下",
        "帮我更新一下那个画图模块",
        "这个插件能更新吗，帮我更一下",
        "把所有扩展统一升级",
        "帮我把那个本事同步到最新版",
        "想给这个扩展打个补丁更新",
        "把装的所有插件都拉新",
        "帮我看看那个模块能不能升到最新",
        "把这个组件的版本往上抬一下",
    ],
    # ===== E. 列出已装/查看清单 =====
    "list": [
        "帮我看看现在都装了哪些扩展",
        "列一下已经加进来的本事",
        "现在 agent 有哪些功能模块",
        "帮我看下装了几个插件",
        "把已安装的扩展都列出来",
        "想知道现在有哪些能力可用",
        "帮我查下都加了哪些扩展",
        "现在装了哪些第三方插件",
        "列个清单看看 agent 的本事",
        "帮我看看已经装上的功能有哪些",
        "想确认下那个扩展装没装上",
        "把当前的扩展清单给我看看",
        "帮我盘点下装了哪些能力",
        "现在有几个扩展在用",
        "帮我列出全局和项目各装了啥",
    ],
    # ===== F. 概念/范围 =====
    "concept": [
        "内置的本事和我自己装的有啥区别",
        "装的扩展放在哪个目录",
        "装完要不要重启才能用",
        "全局装和项目装有什么不一样",
        "我自己装的会覆盖内置的吗",
        "装扩展需要什么环境",
        "这些扩展是从哪来的",
        "装完下次对话就能自动用上吗",
        "扩展装在用户目录还是项目目录",
        "重名的扩展谁优先生效",
        "装第三方扩展安全吗",
        "用户装的和系统自带的怎么区分",
        "扩展不用了会自动清理吗",
        "装的本事会同步到别的设备吗",
        "这个扩展机制是怎么工作的",
    ],
    # ===== G. 装完用不了/排错 =====
    "trouble": [
        "装了那个扩展但好像没生效",
        "插件装上了却调不出来",
        "装完报错了，帮我看看",
        "那个本事装了但 agent 不认",
        "扩展装好了重启也没用",
        "装的插件提示找不到，咋回事",
        "新加的能力没出现在列表里",
        "装扩展的时候失败了",
        "插件装一半卡住了",
        "装好的扩展用起来报错",
        "明明装了怎么还是不能用",
        "扩展装了但功能不对",
        "装的时候提示缺环境，怎么办",
        "插件冲突了，帮我排查",
        "装完之后老的功能也坏了",
    ],
    # ===== H. 从某来源装 =====
    "source": [
        "这个 github 链接的扩展帮我装上",
        "我给你个仓库地址，装里面那个本事",
        "从这个 npm 包装个插件进来",
        "这个 url 的扩展帮我加进项目",
        "我找到一个开源扩展，地址给你帮我装",
        "把这个仓库里指定名字的扩展装上",
        "从这个地址装个全局的能力",
        "这个作者做的插件帮我装一下",
        "我有个扩展的包名，帮我装",
        "从社区那个仓库里挑个扩展装上",
        "这个链接里有好几个扩展，装我要的那个",
        "帮我按这个地址把扩展拉下来装",
        "我贴个 repo，装它提供的本事",
        "从这个开源地址装个工具进来",
        "这个 npm 上的插件帮我加进来",
    ],
    # ===== I. 集成/挂载到 agent 或系统（治串到 deepwiki/lark-shared/channels）=====
    # 本质：把外部来源（代码平台/网上/npm）的「工具/能力」装载挂载到我的 agent / 系统 /
    # 工作流。区分信号：宾语是「给 agent 用的能力扩展」，动作是「集成/挂载/加载」——
    # 不是查仓库文档(deepwiki)，不是 cli 登录配置(lark-shared)。说法自拟，防泄漏。
    "integrate": [
        "把托管在代码平台上的那个工具组件整合进我的助手",
        "我想给当前这套工作流挂载一个外部的处理能力",
        "帮我把网上公开的那个辅助程序加载到运行环境里",
        "能不能把代码仓库里那套功能逻辑集成到我的助理",
        "帮我把这个第三方处理模块装载到对话系统里用",
        "想把社区做的那个增强项接到我的 agent 上",
        "把远程仓库里的功能套件部署进我的运行环境",
        "帮我给系统接一个能处理文档的外部组件",
        "想把那个开源的自动化逻辑引入到我的工作流里",
        "把网上看到的那个好用的能力挂到我的助手上",
        "帮我把一个外部脚本工具集成进当前的任务序列",
        "想给现在的对话逻辑添一个新的流程控制扩展",
        "把代码平台上那个分析组件封装成我助手的一项能力",
        "帮我从远端拉一套工具逻辑装载到本地的运行环境",
        "想让我的 agent 多一个外接的处理本事，帮我接上",
        "把这个公开的组件库引入到我的开发环境里用",
        "帮我把别人写好的那套增强逻辑挂载进系统",
        "想把一个外部来源的能力模块整合到当前助理",
    ],
    # ===== K. 跨界边界（防串 lark-doc / channels / getnote / legal-assistant）=====
    # 关键区分：
    #   "github 仓库里的组件装到 agent" ≠ lark-doc（云文档内容读写）
    #   "把对接逻辑引进来当 agent 能力" ≠ channels（接入飞书事件长连接）
    #   "查 agent 挂了哪些组件" ≠ getnote（个人知识收藏库）
    #   "第三方插件安全顾虑" ≠ others（应属能力管理概念询问）
    "cross_bdry": [
        # 防串 lark-doc：github仓库地址 + 装到agent
        "我这有个 github 仓库地址，里面有个组件叫文档助手，帮我按说明把它配到助手上",
        "给你个 github 链接，里面有几个模块，帮我把想要的那个装进来让助手能用",
        "网上找到个开源小工具，我把地址甩给你，按里面的说明帮我配到助手上",
        "这个仓库里有个处理数据的组件，帮我照着 readme 把它接进来",
        "有个 github 上的工具挺对胃口，帮我按它的接入说明装到助手里",
        "我把这个开源项目的链接给你，帮我把里头那个插件配进来",
        # 防串 channels：内部系统对接逻辑 + 装成agent能力（不是接飞书事件）
        "我们内部有套现成的处理逻辑，帮我把它引进来装成助手的一项能力",
        "别人写好了一段对接代码，帮我把这段挂到助手上让它以后能调",
        "有个内部脚本已经写好了，帮我把它集成进来当助手的一个新本事",
        "网上那个抓价格数据的脚本，帮我弄进来让助手以后能用这手",
        "内部有套数据清洗脚本，想让助手也能用，帮我把它整合进来",
        "同事写了个自动化脚本，帮我把它装成助手的一项能力",
        # 防串 getnote：查助手能力清单（不是个人收藏库）
        "我都忘了给助手配过哪些组件了，帮我把现在挂着的能力全列出来",
        "列一列现在助手到底挂了哪些模块，我记不清了",
        "帮我查下助手当前激活了哪些能力插件",
        "我想核对下助手的能力清单，看看装了什么",
        # 防串 others：第三方插件安全/兼容顾虑（属能力管理概念询问）
        "第三方来的组件配进来之后会不会偷偷访问本地文件",
        "我怕外来插件不安全，帮我确认下它的权限范围",
        "从网上装进来的模块，会不会把我的数据传出去",
        "不熟悉的扩展装进来安不安全，有没有沙箱隔离",
    ],
    # ===== J. 组件报错/装了用不了的边界样本（防串 code-review / others）=====
    # 关键：外壳是「配好了调不出来 / 咋回事 / 修了 bug 出新版 / 清单里看不到」，
    # 内核是对 agent 扩展组件的装/查/更/排错，跟审查代码 diff 无关。
    # 让模型学会「组件装配报错=skills-manager 而非 code-review」。
    "bdry": [
        "那个组件照教程配好了重启也试了，就是死活调不出来，帮我看看",
        "新接上去的模块在清单里明明看不到，可我确实配过，这咋回事",
        "上次配的那个组件出新版了说修了不少 bug，顺手帮我拉到最新",
        "装到一半好像卡住了，现在助手启动都变慢了，是不是那插件把环境搞坏了",
        "换了台电脑，帮我核对下这边挂的组件跟原来那台是不是一样全",
        "那个扩展装完提示缺依赖跑不起来，帮我看看少了啥",
        "配好的模块一调用就报错，帮我排查下是不是版本不对",
        "两个插件好像打架了，装上之后老的功能也坏了，帮我理一理",
        "组件更新完反而用不了了，帮我看看是不是更新出的问题",
        "明明装上了却提示找不到，帮我看看是不是没挂对地方",
        "那个模块装了半天报错中断，帮我看看能不能重装一次",
        "扩展清单里少了一个我记得装过的，帮我查查是不是没生效",
        "配置组件的时候一直失败，帮我看看是不是环境没配好",
        "装的插件版本太老总报警告，帮我升上去看还报不报",
        "接进来的组件突然不工作了，帮我看看是不是要重新加载",
        "组件装好却调不动，是不是得把会话重开一下才认",
        "帮我看看新挂的那个模块为啥没出现在能力列表里",
        "那个扩展装完把别的功能也带崩了，帮我排一排冲突",
    ],
}

# ===================== POOL_VAL：换说法、中等难度 =====================
POOL_VAL: dict[str, list[str]] = {
    "find": [
        "看看社区里有没有现成的组件可以拿来用",
        "有没有能对接飞书的扩展可以挑一个",
        "想物色一个能做语音识别的插件",
        "帮我扒拉下有哪些跟视频剪辑相关的模块",
        "市面上有能生成 PPT 的现成组件吗",
    ],
    "install": [
        "把这个开源组件配置到我的环境里",
        "想给助手补一个能读 word 的模块",
        "帮我把那个做图表的扩展部署上",
        "从远端仓库拉一个爬网页的插件装进来",
        "给它配一套能连微信的功能模块",
    ],
    "remove": [
        "那个占内存的组件帮我下掉",
        "这个模块用不着了，帮我拆掉",
        "把重复安装的那个插件精简掉一个",
        "有个报错的扩展，帮我先禁用并清除",
    ],
    "update": [
        "帮我把手头这个组件刷到最新",
        "看看哪些插件有新版本待升",
        "那个查天气的模块过时了，帮我拉新",
        "把当前环境里的扩展统一同步一遍版本",
    ],
    "list": [
        "帮我盘一盘现在挂了哪些组件",
        "把当前生效的插件清单打出来",
        "想核对下那个模块到底配没配上",
        "全局和项目分别有哪些扩展，帮我理一理",
    ],
    "concept": [
        "系统自带的组件和我后加的优先级谁高",
        "配好的扩展存到哪个路径下",
        "加载完是不是得重开会话才认",
        "第三方来的模块跑起来安不安全",
    ],
    "trouble": [
        "那个组件配好了却怎么都调不起来",
        "扩展加载完提示缺依赖，咋整",
        "新挂的模块在清单里压根看不到",
        "两个插件好像打架了，帮我排一排",
    ],
    "source": [
        "我贴个开源地址，把里面那个组件配进来",
        "从这个包名把对应插件拉下来部署",
        "这位作者写的扩展，帮我按链接接上",
        "从社区那个仓库挑指定的模块配到全局",
    ],
    "integrate": [
        "把代码平台上那套处理逻辑封装成助手的一项本事",
        "想给现在的工作流外挂一个远端的分析组件",
        "帮我把网上公开的那个自动化脚本接进运行环境",
        "把别人写好的增强逻辑引入到当前助理里用",
    ],
}

# ===================== POOL_TEST：最口语、含背景/省略/隐式意图，部分故意写长 =====================
POOL_TEST: dict[str, list[str]] = {
    "find": [
        "我平时得整理一堆发票，想看看有没有那种现成的小组件能帮我自动归类的",
        "诶你们这边能挑的扩展多不多，我想给助手弄个会做读书笔记的",
        "有没有那种一装上就能帮我盯着邮箱有没有新邮件的现成东西",
    ],
    "install": [
        "我刚在网上看到个特别顺手的开源小组件，链接一会发你，你帮我配到助手上让它也能用",
        "老板让我这周把周报自动化，你能不能给它挂个能生成周报的模块",
        "我这项目老要处理 PDF，能不能就单给这个项目配上那个读 PDF 的插件，别的项目不用",
        "网上那个抓股价的脚本挺好，帮我弄进来让助手以后也会这手",
    ],
    "remove": [
        "之前图新鲜装了一堆乱七八糟的组件，现在卡得很，你帮我把没用的都清一清",
        "那个翻译插件老出问题，越用越烦，直接给我拆了吧",
        "我记得前阵子加过一个画流程图的模块，现在用不上了，帮我撤掉",
    ],
    "update": [
        "上次配的那个组件好像出新版了，说是修了不少 bug，你顺手帮我拉到最新呗",
        "我这边一堆插件估计都好久没动了，你统一给我刷新一遍看看",
        "那个连数据库的模块总报警告，是不是版本太老了，帮我升上去试试",
    ],
    "list": [
        "我都有点记不清给助手加过些啥了，你帮我列一列现在到底挂着哪些东西",
        "换了台电脑，想核对下这边的组件跟原来那台是不是一样全，先给我看看清单",
        "全局装的和这个项目单独配的分别有哪些，你帮我分开理清楚点",
    ],
    "concept": [
        "我有点搞不明白，那些系统本来就带的组件，跟我自己后来接上去的，万一撞名了到底听谁的",
        "第三方的东西我总有点怕，配进来会不会偷偷读我本地的文件啊",
        "我今天刚配好一个模块，是不是得把对话重开一下它才生效，还是说下回自动就有了",
    ],
    "trouble": [
        "我昨天照着教程把那个组件配好了，重启也试了，可就是死活调不出来，你帮我看看哪出问题了",
        "新接上去的那个模块，在清单里明明看不到，可我又好像确实配过了，这是咋回事",
        "装到一半好像卡住了，现在助手启动都变慢了，是不是那个插件把环境搞坏了",
    ],
    "source": [
        "我这有个 github 仓库地址，里面好几个组件呢，你就把那个叫做文档助手的给我配上就行",
        "同事发我一个包名，说他们团队都在用这个扩展，你帮我按这个也弄一份到全局",
        "网上翻到个开源项目挺对我胃口，我把链接甩给你，麻烦按里面说明接进来",
    ],
    "integrate": [
        "我们内部代码平台上有套现成的数据清洗逻辑，我想让助手也会这套，你帮我把它整合进来当成它的一项本事",
        "现在这个自动化流程还差个环节，网上正好有个公开的组件能补上，你帮我挂到工作流里",
        "想让我的助理能对接我们那套内部系统，别人已经写好了对接逻辑，你帮我引进来接上",
    ],
}

SUBCAT = {
    "find": ("A", "搜找"),
    "install": ("B", "安装"),
    "remove": ("C", "卸载"),
    "update": ("D", "更新"),
    "list": ("E", "列出"),
    "concept": ("F", "概念"),
    "trouble": ("G", "排错"),
    "source": ("H", "指定来源"),
    "integrate": ("I", "集成挂载"),
    "cross_bdry": ("J", "跨界边界"),
    "bdry": ("K", "组件报错"),
}


def check_no_trigger(text: str) -> None:
    low = text.lower()
    for t in TRIGGERS:
        if t.lower() in low:
            raise AssertionError(f"含 trigger 子串 [{t}]！→ {text}")


def expand_pool(pool) -> list[tuple[str, str]]:
    """基础句 + 语气后缀展开（呢/啊/吗/？），扩充变体量。"""
    out: list[tuple[str, str]] = []
    mood = set("吧呢嘛啊吗了呀哦呐")
    for cat, sents in pool.items():
        for s in sents:
            out.append((s, cat))
            tail = s.rstrip()[-1] if s.strip() else ""
            if tail and tail not in mood and tail not in "？。！，；":
                for suf in ["呢", "啊", "吗", "？"]:
                    out.append((s + suf, cat))
    return out


def dedupe(items, seen=None):
    """去重 + trigger 校验；seen 用于跨池去重（test/val 优先占位，train 让路）。"""
    if seen is None:
        seen = set()
    out = []
    for text, cat in items:
        t = text.strip()
        if not t or t in seen:
            continue
        check_no_trigger(t)
        seen.add(t)
        out.append((t, cat))
    return out


def cap_per_split(items, target_n, seed):
    """打散后截断到目标量，按子语义配额轮转取，避免某类被砍光。"""
    rng = random.Random(seed)
    by_cat = {}
    for text, cat in items:
        by_cat.setdefault(cat, []).append((text, cat))
    for cat in by_cat:
        rng.shuffle(by_cat[cat])
    if len(items) <= target_n:
        out = list(items); rng.shuffle(out); return out
    out, cats = [], list(by_cat.keys())
    idx = {c: 0 for c in cats}
    while len(out) < target_n:
        progressed = False
        for c in cats:
            if idx[c] < len(by_cat[c]) and len(out) < target_n:
                out.append(by_cat[c][idx[c]]); idx[c] += 1; progressed = True
        if not progressed:
            break
    rng.shuffle(out)
    return out


def write_jsonl(path, items):
    with open(path, "w", encoding="utf-8") as fh:
        for text, cat in items:
            code, name = SUBCAT[cat]
            rec = {"text": text, "label": "skills-manager", "subcat": f"{code}-{name}"}
            fh.write(json.dumps(rec, ensure_ascii=False) + "\n")


def main():
    base = Path(__file__).resolve().parent
    # 先建 test/val（优先占位），再建 train（让路，避免任何跨池重复）
    seen: set = set()
    test_raw = dedupe(expand_pool(POOL_TEST), seen)
    val_raw = dedupe(expand_pool(POOL_VAL), seen)
    train_raw = dedupe(expand_pool(POOL_TRAIN), seen)
    print(f"三池展开+跨池去重后：train={len(train_raw)} val={len(val_raw)} test={len(test_raw)}")

    test = cap_per_split(test_raw, 75, seed=20260711)
    val = cap_per_split(val_raw, 75, seed=20260712)
    train = cap_per_split(train_raw, 800, seed=20260713)

    write_jsonl(base / "train" / "skills-manager.jsonl", train)
    write_jsonl(base / "val" / "skills-manager.jsonl", val)
    write_jsonl(base / "test" / "skills-manager.jsonl", test)
    print(f"train={len(train)} val={len(val)} test={len(test)}")
    for name, split in [("train", train), ("val", val), ("test", test)]:
        c = Counter(SUBCAT[cat][0] for _, cat in split)
        print(f"\n{name} 子语义分布（共 {len(split)}）：")
        for cat in SUBCAT:
            code = SUBCAT[cat][0]
            print(f"  {code}-{SUBCAT[cat][1]:<6} {c.get(code, 0):>3}")


if __name__ == "__main__":
    main()